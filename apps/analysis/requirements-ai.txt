# Optional dependencies for connecting LIVE Claude (the Agent SDK) + the MCP server.
# Install these only when you want real Claude verdicts instead of the grounded
# fallback:   pip install -r requirements-ai.txt
#
# Note: the app runs fully without these — claude_runner and mcp_server both
# lazy-import them and degrade gracefully when they're absent.
#
# Versions are left loose so pip can resolve a mutually-compatible set
# (mcp needs anyio>=4.5, which is why these are separated from the base file).
httpx>=0.27
anyio>=4.5
claude-agent-sdk>=0.1.0
mcp>=1.2.0
